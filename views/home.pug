extends layouts/main
include mixins/videoBlock

block content
    each video in videos
        +videoBlock({
            title:video.title,
            views:video.views,
            videoFile:video.videoFile
        })